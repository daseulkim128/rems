<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberSql">

	<insert id="members" parameterType="hashmap">
		<selectKey resultType="string" keyProperty="PWD" order="BEFORE">
			SELECT REPLACE(UUID(),'-','')
      	</selectKey>
		INSERT INTO MEMBERS 
					(	ID
						, PWD
						, NM
						, EML
						, REG_DT
						, MDFCN_DT
						, STAT
						, CONT
					) 
					VALUES	(
						#{ID}
						, #{PWD}
						, #{NM}
						, #{EML}
						, NOW()
						, NOW()
						, #{STAT}
						, #{CONT}
					)
	</insert>
	
</mapper>